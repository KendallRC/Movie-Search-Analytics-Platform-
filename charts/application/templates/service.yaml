{{- if .Values.app.enabled }}

apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.app.name }}-sv
spec:
  selector:
    app: {{ .Values.app.name }}-dp
  ports:
    - protocol: TCP
      port: {{ .Values.app.local_port }}
      targetPort: {{ .Values.app.local_port }}

{{- end }}
